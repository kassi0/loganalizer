[project]
name = "loganalizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cassio Telles", email = "kassio@msn.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.0",
]

[project.scripts]
loganalizer = "loganalizer:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
